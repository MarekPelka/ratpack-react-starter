export interface Product {
	id: string;
	name: string;
	image: string;
	shortDescription: string;
	description: string;
	price: number;
}

export interface Company {
	id: string;
	name: string;
	image: string;
	location: string;
	phone: string;
}